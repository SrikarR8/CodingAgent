# Changes:
- Added the modulo operator (%) to the `Calculator` class.
- Updated `self.operators` dictionary in `Calculator.__init__` to include the modulo operation (`lambda a, b: a % b`).
- Updated `self.precedence` dictionary in `Calculator.__init__` to assign precedence 2 to the modulo operator, consistent with multiplication and division.
